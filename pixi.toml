[workspace]
authors = ["Joris Vaillant <joris.vaillant@inria.fr>"]
channels = ["conda-forge"]
name = "bspline"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
python = "*"
pip = "*"
numpy = "*"
matplotlib = "*"

[pypi-dependencies]
bspline = { path = ".", editable = true }
